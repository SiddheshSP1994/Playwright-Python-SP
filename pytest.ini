[pytest]
addopts = -p plugins.markers_reg

markers =
    smoke: fast/high-value checks
    regression: broad coverage suite
    slow: long-running checks
    flaky: unstable in some conditions
    ai: AI/LLM-assisted flows

python_files = test_*.py *_test.py
testpaths = tests
